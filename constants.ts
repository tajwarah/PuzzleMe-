import type { Difficulty } from './types';

export const DIFFICULTIES: Difficulty[] = [3, 4, 6, 8];

export const IMAGE_MAX_SIZE_MB = 10;
export const IMAGE_MIN_DIMENSION = 512;

export const SNAP_RADIUS = 20; // in pixels
